/* ================= DELAY FUNCTION PROTOTYPES ================= */

/*
 * Generates a delay in microseconds
 */
void delay_us(unsigned int);

/*
 * Generates a delay in milliseconds
 */
void delay_ms(unsigned int);

/*
 * Generates a delay in seconds
 */
void delay_s(unsigned int);
