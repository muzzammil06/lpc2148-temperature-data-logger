/*
 * Configures the function of a microcontroller pin
 * Parameters:
 *   1st int ? Port number
 *   2nd int ? Pin number
 *   3rd int ? Function selection value
 */
void CfgPinFunc(int, int, int);
